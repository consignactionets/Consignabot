version: '3.8'

services:
  consignabot:
    build: .
    container_name: consignabot
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
    volumes:
      - ./data:/app/data
    restart: unless-stopped